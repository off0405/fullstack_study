module.exports
require('경로')

crypto.creatHash().update().digest()

fs모듈 : const fs = require('fs')



✔ 안전한 서버 이어주기
  res.sendFile(path.join(__dirname, '/index.html'))


 라우터 : 원래는 전화 교환기와 비슷하게 연결 경로를 자동으로 전환하여 주는 기계를 라우터

 params 키워드란 메소드에서 배열형태의 매개변수를 받을때 사용하는 키워드 입니다.
 즉 params는 배열.